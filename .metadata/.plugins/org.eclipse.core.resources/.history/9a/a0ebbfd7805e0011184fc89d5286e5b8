
public class FuncoesMatematicasExemplo {

	public static void main(String[] args) {
		
		double delta = Math.pow(b, 2.0) - 4*a*c;
		x1 = (-b + Math.sqrt(delta)) / (2.0 * a);
		x2 = (-b - Math.sqrt(delta)) / (2.0 * a);

	}

}
